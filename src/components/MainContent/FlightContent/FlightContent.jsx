import React from "react";
import ComingSoon from "../ComingSoon/ComingSoon";

const FlightContent = (props) => {
  return <ComingSoon activePage={props.activePage} />;
};

export default FlightContent;
